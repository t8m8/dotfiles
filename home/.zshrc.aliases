if [ "$(uname)" = 'Darwin' ]; then
  alias ls='ls -G'
else
  alias ls='ls --color=auto'
fi
alias sl='ls'
 
alias 'browse'='(){ open `git config --get remote.origin.url` }'

alias 'password'='(){ lpass show $1 --password }'
